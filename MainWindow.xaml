<Window x:Class="Loopback.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Loopback.Properties"
        Title="Loopback Exemption Manager" Height="549.196" Width="612.53" Closing="Window_Closing" Icon="assets/Windows.png">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Strings;component" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center">
            <Button Name="btnLanguage" Grid.Row="0" Width="80" Height="30" Click="btnLanguage_Click" Margin="10,0" Content="中文"/>
            <Button Name="btnSelectAll" Grid.Row="0" Width="100" Height="30" Click="btnSelectAll_Click" Margin="10,0" Content="Select All"/>
            <Button Name="btnDeselectAll" Grid.Row="0" Width="100" Height="30" Click="btnDeselectAll_Click" Margin="10,0" Content="Deselect All"/>
            <Button Name="btnSave" Grid.Row="0" Width="100" Height="30" Click="btnSave_Click" Margin="10,0" Content="Save"/>
            <Button Name="btnRefresh" Grid.Row="0" Width="100" Height="30" Click="btnRefresh_Click" Margin="10,0" Content="Refresh"/>

        </StackPanel>
        <DataGrid x:Name="dgLoopback" AutoGenerateColumns="False" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Exempt" Width="60" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox Name="dgcbLoop" IsChecked="{Binding LoopUtil, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="dgcbLoop_Click" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="App Name" Binding="{Binding Path=displayName}" />
            </DataGrid.Columns>
        </DataGrid>
        <StatusBar x:Name="SBar" Grid.Column="0" Grid.Row="3" VerticalAlignment="Bottom" Background="LightBlue" DockPanel.Dock="Bottom" Height="30">
            <StatusBarItem>
                <TextBlock FontWeight="Bold"><Run Text="Status: "/></TextBlock>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <TextBlock Name="txtStatus"></TextBlock>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
